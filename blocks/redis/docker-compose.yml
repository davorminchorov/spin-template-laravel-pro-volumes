services:

  redis:
    # Valkey is Redis-compatible; the service keeps the "redis" name so REDIS_HOST=redis works
    image: valkey/valkey:9-alpine
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s