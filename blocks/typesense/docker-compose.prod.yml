services:

  typesense:
    volumes:
      - typesense_data:/data
    networks:
      - web-public
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 5s
        order: start-first

volumes:
  typesense_data:
