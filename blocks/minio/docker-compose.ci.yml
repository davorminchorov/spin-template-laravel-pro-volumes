services:
  minio:
    environment:
      MINIO_ROOT_USER: "ciuser"
      MINIO_ROOT_PASSWORD: "cipassword"
  minio-setup:
    environment:
      MINIO_ROOT_USER: "ciuser"
      MINIO_ROOT_PASSWORD: "cipassword"
      MINIO_BUCKET: "ci"
